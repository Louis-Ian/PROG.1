#include <stdio.h>

struct registro{
	char nome[101];
	char tipo[2];
	long long int matricula;
	long long int telefone;
	int ddd;
	struct registro* prox;
};
typedef struct registro Registro;

void inserirlista(struct registro **agenda,struct registro novo){
	prox.novo=prox.(*agenda);
	if (prox.)
	prox.(*agenda)=novo;
	
}
